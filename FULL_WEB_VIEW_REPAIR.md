# ORION-9 Full Web View Repair

Updated `src/components/Profile.tsx` so Account & Settings uses the full available application viewport instead of a centered 1100px content column.

- Header spans the available width.
- Tab navigation spans the available width.
- Main content spans the available width.
- Existing responsive padding and internal field layouts are preserved.
- No functional profile behavior was changed.
